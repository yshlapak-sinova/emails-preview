<style type='text/css'>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700&display=swap');

    /* Reset */
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    /* Base Styles */
    body {
        font-family: 'Poppins', Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #e8eae8;
        min-height: 100vh;
        -webkit-text-size-adjust: 100%;
        -ms-text-size-adjust: 100%;
    }

    /* Container Layout - Desktop (default) */
    .email-container {
        width: 798px;
        max-width: 90%;
        background: #ffffff;
        border-radius: 40px;
        box-shadow: 2px 0px 14px rgba(109, 109, 109, 0.17);
        overflow: hidden;
        margin: 20px auto;
        /* Email compatibility: use table-based layout */
        display: block;
    }

    .content-wrapper {
        padding: 44px 44px 0 44px;
        /* Email compatibility: remove flexbox */
        display: block;
    }

    .content {
        width: 710px;
        max-width: 100%;
        /* Email compatibility: remove flexbox, use block */
        display: block;
        padding-bottom: 64px;
    }

    .main-content {
        /* Email compatibility: remove flexbox */
        display: block;
        text-align: center;
    }

    .logo-wrapper {
        height: 42px;
        padding: 12px 116px;
        /* Email compatibility: remove flexbox */
        display: block;
        text-align: center;
    }

    .logo {
        width: 152px;
        height: 18.85px;
        display: inline-block;
    }

    .text-content {
        /* Email compatibility: remove flexbox */
        display: block;
        width: 100%;
        text-align: left;
    }

    .h1 {
        font-weight: 600;
        font-size: 36px;
        line-height: 44px;
    }

    svg {
        overflow: visible;
    }

    .title {
        font:
            500 24px/32px 'Poppins',
            Arial,
            sans-serif;
        color: #1c1c1c;
        height: 34px;
        text-align: left;
    }

    .code {
        font:
            700 48px/60px 'Poppins',
            Arial,
            sans-serif;
        color: #1c1c1c;
        letter-spacing: -0.12px;
        width: 178px;
        height: 58px;
        text-align: left;
    }

    .body-text {
        font:
            400 18px/28px 'Poppins',
            Arial,
            sans-serif;
        color: #1c1c1c;
        text-align: left;
    }

    .support-section {
        width: 710px;
        max-width: 100%;
        /* Email compatibility: remove flexbox */
        display: block;
    }

    .support-text {
        font:
            400 18px/28px 'Poppins',
            Arial,
            sans-serif;
        color: #1c1c1c;
        height: 29px;
        text-align: left;
    }

    .support-text a {
        color: #1c1c1c;
        text-decoration: underline;
    }

    .center-link {
        display: block;
        text-align: center;
        margin-left: auto;
        margin-right: auto;
        font-weight: 600;
        font-size: 14px;
        line-height: 24px;
        letter-spacing: -0.5%;
        text-decoration: underline;
    }
    
    .button {
        border-radius: 6px;
        /* Email compatibility: remove flexbox */
        display: inline-block;
        text-decoration: none;
        text-align: center;
        font-size: 14px;
        font-weight: 600;
        line-height: 24px;
        letter-spacing: -0.5%;
        width: 240px;
        height: 40px;
        margin-left: auto;
        margin-right: auto;
        /* Email compatibility: ensure proper button display */
        padding: 8px 16px;
    }

    .button-primary {
        background: #F28C28;
        color: #ffffff;
    }

    .button-secondary {
        background: #ffffff;
        color: #F28C28;
        border: 1px solid #F28C28;
    }

    .footer {
        width: 100%;
        background: #f3f3f3;
        border-bottom-left-radius: 40px;
        border-bottom-right-radius: 40px;
        /* Email compatibility: remove flexbox */
        display: block;
        text-align: center;
    }

    .footer a {
        color: #1c1c1c;
        text-decoration: underline;
        font-weight: 600;
    }

    .footer-content {
        width: 710px;
        max-width: 100%;
        /* Email compatibility: remove flexbox */
        display: block;
        text-align: center;
    }

    .footer-text {
        font:
            400 14px/20px 'Poppins',
            Arial,
            sans-serif;
        color: #1c1c1c;
        text-align: left;
    }

    .copyright {
        font:
            400 12px/18px 'Poppins',
            Arial,
            sans-serif;
        color: #1c1c1c;
        letter-spacing: 0.06px;
    }

    /* Tablet Styles (768px - 1199px) */
    @media screen and (min-width: 768px) and (max-width: 1199px) {
        .email-container {
            width: 688px;
            border-radius: 20px;
        }

        .content {
            width: 600px;
        }

        .support-section {
            width: 600px;
        }

        .footer-content {
            width: 600px;
        }

        .body-text {
            line-height: 26px;
        }

        .support-text {
            line-height: 26px;
        }

        .footer {
            border-bottom-left-radius: 20px;
            border-bottom-right-radius: 20px;
        }
    }

    /* Mobile Styles (≤767px) */
    @media screen and (max-width: 767px) {
        .email-container {
            width: 288px;
            border-radius: 16px;
        }

        .content-wrapper {
            padding: 16px 12px 0 12px;
        }

        .content {
            width: 264px;
        }

        .main-content {
            /* Email compatibility: remove gap property */
        }

        .logo {
            width: 111.67px;
            height: 13.85px;
        }

        .title {
            font:
            500 18px/26px 'Poppins',
            Arial,
            sans-serif;
            text-align: center;
        }

        .code {
            font:
            700 40px/48px 'Poppins',
            Arial,
            sans-serif;
            letter-spacing: -0.2px;
            text-align: center;
            width: auto;
            height: auto;
        }

        .body-text {
            font:
            400 14px/22px 'Poppins',
            Arial,
            sans-serif;
        }

        .support-section {
            width: 264px;
            height: 70px;
        }

        .support-text {
            font:
            400 14px/22px 'Poppins',
            Arial,
            sans-serif;
            height: 70px;
            width: 263px;
        }

        .footer {
            border-bottom-left-radius: 16px;
            border-bottom-right-radius: 16px;
        }

        .footer-content {
            width: 264px;
        }

        .footer-text {
            font:
            400 12px/18px 'Poppins',
            Arial,
            sans-serif;
        }

        .copyright {
            font:
            400 12px/18px 'Poppins',
            Arial,
            sans-serif;
            letter-spacing: 0.06px;
        }
    }

    /* Email-specific compatibility fixes */
    
    /* Force table layout for better email client support */
    table {
        border-collapse: collapse;
        mso-table-lspace: 0pt;
        mso-table-rspace: 0pt;
    }
    
    /* Outlook-specific fixes */
    .ExternalClass {
        width: 100%;
    }
    
    .ExternalClass,
    .ExternalClass p,
    .ExternalClass span,
    .ExternalClass font,
    .ExternalClass td,
    .ExternalClass div {
        line-height: 100%;
    }
    
    /* Prevent Gmail from changing the text color in conversation threads */
    .im {
        color: inherit !important;
    }
    
    /* Force Outlook to provide a "view in browser" message */
    #outlook a {
        padding: 0;
    }
    
    /* Prevent WebKit and Windows mobile changing default text sizes */
    body {
        width: 100% !important;
        -webkit-text-size-adjust: 100%;
        -ms-text-size-adjust: 100%;
    }
    
    /* Better image handling */
    img {
        -ms-interpolation-mode: bicubic;
        border: 0;
        height: auto;
        line-height: 100%;
        outline: none;
        text-decoration: none;
    }
    
    /* Remove spacing around tables */
    table td {
        border-collapse: collapse;
    }
    
    /* iOS BLUE LINKS */
    a[x-apple-data-detectors] {
        color: inherit !important;
        text-decoration: none !important;
        font-size: inherit !important;
        font-family: inherit !important;
        font-weight: inherit !important;
        line-height: inherit !important;
    }
    
    /* Gmail blue links */
    u + #body a {
        color: inherit;
        text-decoration: none;
        font-size: inherit;
        font-family: inherit;
        font-weight: inherit;
        line-height: inherit;
    }
    
    /* Samscreen email client */
    #MessageViewBody a {
        color: inherit;
        text-decoration: none;
        font-size: inherit;
        font-family: inherit;
        font-weight: inherit;
        line-height: inherit;
    }

    {{!-- Utility Classes --}}

    .u-no-underline {
        text-decoration: none !important;
    }

    .u-weight-400 {
        font-weight: 400 !important;
    }

    .signature {
        font-weight: 400;
        font-style: italic;
        font-size: 16px;
        line-height: 28px;
        letter-spacing: 0%;
    }
    
    /* Email-specific spacing utilities */
    .spacer {
        height: 24px;
        line-height: 24px;
        font-size: 1px;
    }
    
    .spacer-42 {
        height: 42px;
        line-height: 42px;
        font-size: 1px;
    }
    
    .spacer-64 {
        height: 64px;
        line-height: 64px;
        font-size: 1px;
    }
</style>